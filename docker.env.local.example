# Copy this file to "docker.env.local" and
# change values according to your needs.

VITE_CG_DATA_REFRESH_INTERVAL="60000"
VITE_PLUGIN_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----<key>-----END PUBLIC KEY-----\n"
PLUGIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----<key>-----END PRIVATE KEY-----\n"
COOKIE_SECRET="your_cookie_secret"
VITE_URL_PREFIX="/"

# License Registry Configuration
# ENS name that points to the license registry (e.g., "license.commonground.eth")
VITE_LICENSE_ENS_NAME=""
# Optional: Direct CID for development (bypasses ENS resolution)
VITE_REGISTRY_CID=""
# Optional: Custom IPFS gateways (comma-separated, defaults to public gateways)
# VITE_IPFS_GATEWAYS="https://dweb.link,https://w3s.link,https://cloudflare-ipfs.com"
# Optional: Storage protocol (ipfs, bzz) - defaults to "ipfs"
# VITE_STORAGE_PROTOCOL="ipfs"